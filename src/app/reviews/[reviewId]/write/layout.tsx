import React from "react";

export const metadata = {
  title: "리뷰 등록",
  description: "와인 리뷰 등록 페이지",
};

export default function WriteLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
